Crear una función con el siguiente prototipo:<br>

``` c
void darVuelta(char * original, char * dadoVuelta);
```

La misma va a tomar la cadena `original` y va a guardar una copia dada vuelta en `dadoVuelta`.<br>

